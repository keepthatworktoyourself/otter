{
  "scripts": {
    "i": "rm -rf node_modules && npm i",
    "clean": "rm -rf dist",
    "build": "npm run clean && parcel build blockset.js",
    "build-dev": "npm run clean && NODE_ENV=development parcel build --no-minify blockset.js",
    "dev": "npm run clean && parcel watch blockset.js --no-hmr"
  },
  "dependencies": {
    "otter-editor": "0.0.18",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "@babel/core": "^7.10.3"
  }
}
