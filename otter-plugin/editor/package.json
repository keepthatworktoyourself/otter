{
  "name": "iceberg-wp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "i": "rm -rf node_modules && npm i",
    "build": "rm -rf dist && parcel build index.html",
    "build-dev": "rm -rf dist && NODE_ENV=development parcel build --public-url=. --no-minify index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "iceberg-editor": "0.0.14",
    "react": "^16.13.1",
    "react-color": "^2.18.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "@babel/core": "^7.10.3"
  }
}
